# Pilotos controlados de certificación — cliente 23

Herramienta preparada para revisión operacional. **No ejecutar `--pilot` hasta verificar el candidato, configuración, campaña y fixtures.** No instala servicios, no aplica migraciones ni crea/activa campañas. No automatiza los 279 casos del portal.

Desde la raíz:

```powershell
dotnet run --project tools/ClientCertificationRunner -c Release -- --self-test
dotnet run --project tools/ClientCertificationRunner -c Release -- --preview
dotnet run --project tools/ClientCertificationRunner -c Release -- --preview --campaign 5f681bfb-149d-4650-9a1b-5723feeebef3
dotnet run --project tools/ClientCertificationRunner -c Release -- --rehearse-clone
```

`--preview` es el modo predeterminado, sólo lectura de SQL y generación local. No carga contenedores de servicios, claves, firmadores ni transporte. Exige SQL Server 16 local, NeoSTP_Cloud con las 91 migraciones exactas del ensamblado, empresa 23/NIT esperado/ACTIVA/PRUEBAS, CSV exacto `01,03,11,14` y territorio verificado `05/24/15`. Lee las opciones JSON de la API publicada y aplica **solamente en memoria del runner** `Dte:EsquemaNuevo=true`, aprobado para probar las versiones actuales 01v2/03v4/11v3/14v2 cuyos esquemas locales están disponibles. No modifica archivos de configuración ni el proceso activo. La evidencia registra el override y su hash. Los esquemas locales deben corresponder a la versión efectivamente generada y no pueden resolver referencias externas. Cualquier error en uno de los cuatro previews produce `Passed=false`.

`--rehearse-clone` cambia exclusivamente a la base restaurada indicada en el manifiesto fijo `tmp/client-certification-release-2026-09-05/clone-manifest.json`. Valida nombre `NeoProductionAudit_<GUID>`, identidad local, origen y restauración verificados, además de todas las condiciones anteriores. Necesita la campaña preparada en el clon. Crea cuatro borradores y consumos reales con correlativos dentro de **una transacción que siempre revierte**. Genera y valida los cuatro JSON en memoria y verifica que cantidades y correlativos vuelvan a sus valores anteriores. No usa host, claves, firma ni transporte.

La campaña piloto debe ser provisionada previamente por soporte: GUID reservado `5f681bfb-149d-4650-9a1b-5723feeebef3`, EmpresaId 23, NIT esperado, ambiente PRUEBAS, estado ACTIVE, periodo UTC vigente, MatrixReference `CLIENT23-PILOT-4-V1`, presupuesto total 4 y exactamente 1 para cada tipo 01/03/11/14.

El modo de envío exige tres elementos explícitos: `--pilot TIPO --campaign GUID --execute-test-pilot`. Confirma metadata de credenciales del cliente, firmador HaciendaCert y cliente HTTP de pruebas. El presupuesto y borrador se guardan atómicamente bajo Serializable/applock antes de llamar a `DteDocumentosService.EnviarAsync`. No cambia derechos comerciales ni módulos. Las claves estables por campaña/tipo y la huella de fixture permiten leer una reserva anterior; **una reejecución jamás continúa enviando un borrador previamente comprometido**, incluso si no llegó a firmarse. Ese caso requiere revisión operacional independiente.

HaciendaCert utiliza un XML `CertificadoMH` con PKCS#8 sin cifrar; no necesita contraseña de certificado PFX. Antes de reservar se valida la raíz XML, se rechazan DTD/referencias externas y se verifica el par RSA mediante un desafío aleatorio local RS512, sin firmar un DTE. También se comprueba que la contraseña MH pueda descifrarse con el mismo propósito DataProtection, sin generar claves nuevas, registrar secretos ni autenticar contra Hacienda. La evidencia informa únicamente resultados booleanos.

El transporte aislado permite sólo POST HTTPS hacia `apitest.dtes.mh.gob.sv/seguridad/auth` y `/fesv/recepciondte`, sin redirecciones, cookies ni reintentos, con máximo una autenticación y una recepción por ejecución. Revalida campaña/empresa antes de cada solicitud. Bloquea correo y descarta la creación de notificaciones DTE por webhook. No inicia workers. Un resultado ambiguo conserva el DTE para conciliación y no habilita otro envío.

Los receptores y conceptos son claramente sintéticos. Los datos fiscales reales del emisor permanecen en memoria y, solamente en piloto autorizado, en el DTE persistido y su envío a Hacienda. La evidencia bajo `tmp/client-certification-runner/` expone estados/códigos/identificadores internos, nunca JSON fiscal, contraseñas ni certificados. Aceptación local con sello no se contabiliza automáticamente como avance oficial: se debe comprobar el portal.

El fixture V2 inicializa las versiones 2/4/3/2 para 01/03/11/14. La factura 01 de $1.13 no identifica al receptor con DUI inventado: ambos campos documentales son nulos según el esquema local. La huella de nuevas reservas incluye versión y esos campos. El wrapper permite regenerar un documento con versión persistida anterior; valida la versión del JSON y su esquema, y deja al servicio fiscal sincronizarla en su transacción antes de firmar y transmitir.

Antes del primer piloto14 se revisó su receptor: utiliza tipo37 y número `CERT-SE-PILOT-001`, un pasaporte expresamente sintético para PRUEBAS, conservando nombre y concepto de certificación. Esta revisión sólo cambia la huella del tipo14 (incluye ambos campos); conserva la versión global y los fixtures01/03/11 ya reservados. La admisión por esquema local no demuestra aceptación del número en Hacienda.

Existe una excepción operacional revisada exclusivamente para el rechazo del DTE 1016: `--retry-reviewed-1016 --campaign 5f681bfb-149d-4650-9a1b-5723feeebef3 --expected-rejection E0D6E7D219846E70B0983FCA0B30C0A36CD5D28B0523F550B285A8C366036316 --execute-test-pilot`. No ejecutar sin revisión del operador. Exige respuesta exacta SHA256 UTF16LE, HTTP400/ERROR096, sin sello, y diagnóstico CORREGIR_DATOS sin necesidad de consulta. Bajo Serializable y applock 23 preserva la respuesta original en ocurrencias, registra el marcador único lógico CLIENT_PILOT_RETRY_1016_V1 y corrige únicamente la identificación del receptor a nulo. Mantiene UUID, control, consumo y ambas huellas del ledger original. No reserva otro cupo ni modifica manualmente la versión, firma o respuesta anterior: el servicio fiscal las regenera. Cualquier repetición tras el marcador es sólo lectura, incluso si la ejecución anterior se interrumpió. La evidencia final comprueba identidad y ledger conservados. Los rechazos definitivos y los resultados ambiguos se distinguen mediante DteDiagnosticoGuia.

El segundo reintento revisado está segregado y limitado al DTE1017/CCF03v4, rechazado HTTP400/ERROR009 por NIT sintético inexistente. Exige `--retry-reviewed-1017 --campaign 5f681bfb-149d-4650-9a1b-5723feeebef3 --expected-rejection 20AA343DED877599F64076749D6E15EC079E14A75A34139EAEDC5A9385242FE5 --receiver-snapshot 317182F1ABD7FE71963FF846D03E7F529E130A163059CFCAEC07687D729AE677 --execute-test-pilot`. No ejecutar antes de revisión operacional. Lee únicamente campos fiscales públicos de NEO Empresa2, sin seguimiento EF ni secretos; exige coincidencia exacta con el snapshot autorizado y su hash UTF8. Los códigos06/23 están confirmados en el catálogo activo y Ayutuxtepeque03 en el Excel oficial local (Hoja1 fila158, SHA256 E86D7EDC503D876564CD2BF9B251FB100F838199330C0C513048F4075669B2C6). Copia esos campos sólo al receptor del documento PRUEBAS1017, mantiene el concepto explícito de prueba sin venta y valida el JSON contra esquema03v4 antes de confirmar el marcador CLIENT_PILOT_RETRY_1017_V1. No cambia NEO, el fixture03 global, el DTE1016 aceptado ni sus huellas. Preserva rechazo, UUID, control y ledger, comprueba NEO sin cambios después del pipeline y devuelve lectura en toda repetición del marcador. No hay ejecución automática de este modo.
